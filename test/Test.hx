
import timestamp.Timestamp;

class Test {
        
    static function main() {

        trace('timestamp ' + Timestamp.now());
        trace('timestamp ' + Timestamp.now());
        trace('timestamp ' + Timestamp.now());

    }

}